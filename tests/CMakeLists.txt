if(CMAKE_PROJECT_NAME STREQUAL PROJECT_MAIN_NAME AND BUILD_TESTS)
	if (BUILD_UNIT_TESTS)
		add_subdirectory(Common/Unit)
	endif()
	if (BUILD_INTEGRATION_TESTS)
		add_subdirectory(Common/Integration)
	endif()
endif()
